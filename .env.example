# Copiez ce fichier en .env.local et remplacez les valeurs par les vôtres
# Ne jamais commiter le fichier .env.local !

# ===================================
# Configuration requise
# ===================================

# Clé API OpenAI (obtenue sur https://platform.openai.com/account/api-keys)
# Ne jamais partager ou commiter cette clé !
OPENAI_API_KEY=sk-votre_cle_api_openai_ici

# URL de base de l'application (sans / à la fin)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ===================================
# Configuration optionnelle
# ===================================

# Activer le mode debug (true/false)
NEXT_PUBLIC_DEBUG=false

# Configuration du thème par défaut (light/dark/system)
NEXT_PUBLIC_DEFAULT_THEME=system

# ===================================
# Configuration avancée
# ===================================
# Les variables ci-dessous ont des valeurs par défaut et sont généralement
# inutiles à modifier sauf cas spécifique
NEXT_PUBLIC_API_URL=/api

# Configuration Supabase (optionnel pour les fonctionnalités futures)
# NEXT_PUBLIC_SUPABASE_URL=votre_url_supabase
# NEXT_PUBLIC_SUPABASE_ANON_KEY=votre_cle_anonyme_supabase

# Clé secrète pour les sessions (générer avec : openssl rand -base64 32)
# SECRET_KEY=votre_cle_secrete_ici
