/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com'], // Ajoutez d'autres domaines si nécessaire
  },
};

module.exports = nextConfig;
