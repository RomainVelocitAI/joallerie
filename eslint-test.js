// Fichier de test pour ESLint

// Violation intentionnelle de la règle no-console
console.log('Ceci est un test');

// Variable non utilisée
const unusedVariable = 'test';

// Fonction avec any
export function testFunction(arg) {
  return arg;
}
